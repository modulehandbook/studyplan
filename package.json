{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "sudo docker-compose -f docker-compose.test.yml up -d && jest --runInBand ; sudo docker-compose -f docker-compose.test.yml down ; rm -r tmp-test/",
    "lint": "./node_modules/.bin/eslint --fix .",
    "lint-all": "npm run lint && cd client && npm run lint",
    "clean-packages": "rm package-lock.json && rm -r node_modules && npm i",
    "clean-packages-all": "npm run clean-packages && cd client && npm run clean-packages",
    "clean-docker-packages": "sudo docker-compose down ; sudo docker rm -f $(sudo docker ps -a -q) ; sudo docker volume rm $(sudo docker volume ls -q) ; sudo docker-compose build --no-cache ; sudo docker-compose up"
  },
  "author": "Julia Zamaitat, Project-Team (see README)",
  "license": "ISC",
  "devDependencies": {
    "eml-parser": "^1.0.6",
    "eslint": "^8.6.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-jest": "^25.3.4",
    "eslint-plugin-prettier": "^4.0.0",
    "jest": "^27.4.7",
    "mongodb": "^4.3.0",
    "nodemon": "^2.0.15",
    "prettier": "^2.5.1",
    "supertest": "^6.1.6"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "bcryptjs": "^2.4.3",
    "connect-history-api-fallback": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "googleapis": "^92.0.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.1.5",
    "node-localstorage": "^2.2.1",
    "nodemailer": "^6.7.2",
    "path": "^0.12.7",
    "serve-static": "^1.14.1"
  }
}
